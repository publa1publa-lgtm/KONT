import en from "@/messages/en.json";

/**
 * English-only copy for landing demo chrome (hero orbit, product widgets).
 * Marketing headlines/CTAs still use the active locale via useMessages().
 */
export const landingDemoEn = en.landing;
